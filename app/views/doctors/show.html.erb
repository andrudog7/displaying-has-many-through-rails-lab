<h1><%=@doctor.name%>'s Appointments</h1>
<ul>
    <%@doctor.appointments.each do |appointment|%>
    <li>
        <%=link_to appointment.patient.name, patient_path(appointment.patient)%> - <%=appointment.formatted_date%>
    </li>
    <%end%>
</ul>